<h1>My tasks</h1>

    <% @tasks.each do |task| %>
      <div>
        <input type="checkbox">
         <%=link_to task[:title], task_path(task.id)%>
      </div>
    <% end %>

<p><%= link_to 'Add new task', tasks_new_path %></p>


